# Environment Configuration
PORT=5000

# Email Configuration
# Set to 'true' to enable actual email sending in production
EMAIL_ENABLED=false

# SMTP Configuration (Required when EMAIL_ENABLED=true)
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASS=your-app-password

# Note: When EMAIL_ENABLED=false, emails are logged to console instead of being sent
