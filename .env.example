# Environment Configuration
PORT=5000

# Email Configuration (Optional - for production)
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASS=your-app-password

# Note: In demo mode, emails are logged to console instead of being sent
